# 更新日誌 (ChangeLog)

本專案遵循語意化版本（Semantic Versioning）規範，記錄各版本之新增功能、問題修復、介面優化與架構調整。

---

## [1.1.16] - 2026-08-19

### 🎨 全站動態品牌與 .env.local 完整聯動 (Dynamic Branding & Env Sync)
- **動態品牌解析器 (`getAppBrandConfig`)**：
  - 建立全站統一的品牌、管理者稱呼與客服信箱讀取器，遵循「1. 後台資料庫設定 ➜ 2. `.env.local` ➜ 3. 系統內建預設值」三層回退架構。
- **全站前台頁面全面串接**：
  - 頂部導航 (`Navbar`)、全站頁尾 (`Footer`)、首頁 (`HomePage`)、應用政策 (`About`)、隱私權 (`Privacy`)、條款 (`Terms`)、審核中 (`Pending`) 與註冊頁 (`Register`) 全部無縫串接動態品牌與客服信箱。
- **公開品牌端點 (`GET /api/system/brand`)**：
  - 提供輕量公開 API，供客戶端頁面即時取得最新品牌與客服信箱，免重新編譯即可即時反映變更。
- **Docker 環境變數傳遞修復**：
  - 解決 `docker-compose.yml` 預設展開覆蓋 `env_file` 的衝突，確保 `.env.local` 之 `NEXT_PUBLIC_APP_NAME`、`NEXT_PUBLIC_CONTACT_EMAIL` 與 `ADMIN_EMAILS` 100% 正確注入容器。

---

## [1.1.15] - 2026-08-19

### 📂 原始碼結構全面收納至 `src/` (Source Code Migration)
- **原始碼集中管理 (Source Code Encapsulation)**：
  - 將前端頁面與 API（`app/`）、UI 元件（`components/`）、DDD 領域層與用例（`lib/`）、型別定義（`types/`）以及認證與路由網關（`auth.ts`, `auth.config.ts`, `proxy.ts`）全面遷移至 `src/` 目錄中。
- **工程與運行環境根目錄隔離 (Root Tooling Isolation)**：
  - 根目錄專注保留工程設定檔（`package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `eslint.config.mjs`, `.env.local`, `docker-compose.yml`, `Dockerfile`），結構極致整潔。
- **路徑解析零破壞相容 (Path Alias Resolution)**：
  - 更新 `tsconfig.json` 的路徑別名為 `"@/*": ["./src/*"]`，全站程式碼之 `@/` 引用 100% 零破壞相容。
- **全套測試與打包驗證**：
  - Next.js 16 (Turbopack) 零錯誤打包 32 個路由，全套 135 項單元、安全與整合測試持續 100% 綠燈通過，Docker 容器重啟後健康回應 `HTTP 200 OK`。

---

## [1.1.14] - 2026-08-19

### 🏛️ DDD 領域驅動架構與全域動態設定 (DDD & System Settings)
- **DDD 領域驅動模組化重構 (Domain-Driven Design)**：
  - 劃分 5 大核心業務領域（`Identity`, `Invitation`, `Media`, `Audit`, `Notification`）與 Application Use Cases，將商業邏輯與 API 控制器完整解耦。
- **管理後台全域動態設定頁面 (`/admin/settings`)**：
  - 支援於網頁介面動態修改網站品牌名稱、管理員稱呼、YouTube API Key、SMTP/Resend 郵件帳密與安全防禦策略，免重啟 Docker 容器即時生效。
  - 實作三層優先級解析（1. 後台資料庫設定 ➜ 2. `.env` 環境變數 ➜ 3. 系統內建預設值），支援「留空即回退」與機密金鑰眼睛遮罩，並提供一鍵寄送測試信工具。
- **Docker Compose 預設全套整合與三級階梯式設定檔**：
  - `docker-compose.yml` 預設整合本地 MongoDB 7 容器（自帶健康檢查與 Volume 持久化），新手執行 `docker compose up -d` 即可開箱即用，同時無縫相容自備外部資料庫。
  - `.env.local.example` 重構為三級視覺階梯結構，大幅精簡冷啟動必備變數。
- **自動化測試擴充與 Standalone Docker E2E 模擬 (135/135 PASS)**：
  - 新增 `tests/unit/ddd-domains.test.ts` 與 `tests/unit/system-settings-usecase.test.ts`。
  - 實作 `scripts/test-docker-standalone.sh`，完成獨立 Docker 內建 Mongo 容器端到端整合測試驗證。

---

## [1.1.13] - 2026-08-19

### 📜 系統活動日誌永久全量保存 (Audit Logs)
- **活動日誌升級為「永久全量留存」策略**：
  - 關閉後端自動修剪限制，每一筆使用者登入、登出、註冊申請、兌換解鎖、安全鎖定與管理員審核紀錄 100% 永久留存於 MongoDB。
  - 後台日誌標籤更新為「🟢 永久全量保存」，維持不可手動清空與防篡改特性。

---

## [1.1.12] - 2026-08-19

### 🔒 邀請碼防暴力猜測與安全鎖定 (Security & Rate Limiting)
- **多維度 Session Email + Client IP 安全鎖定**：
  - 10 分鐘內連續輸入錯誤邀請碼達 5 次時，立即觸發 15 分鐘安全冷卻鎖定（HTTP 429）。
  - 對使用者維持安全簡潔提示（不向外部透露倒數秒數與剩餘次數，防止惡意探測）。
  - 於後台活動日誌中完整精確記錄每一次輸錯、觸發鎖定與冷卻期間的攔截軌跡。
  - 使用者成功輸入正確邀請碼完成兌換或註冊後，失敗計數與鎖定狀態立即自動歸零。

---

## [1.1.11] - 2026-08-19

### 🧪 測試日誌前綴沙盒隔離加固 (TDD Isolation)
- **TDD 測試日誌沙盒隔離與精確 Teardown**：
  - 單元測試日誌採用專屬時間戳前綴沙盒隔離，執行完畢 100% 精確抹除測試假資料，絕不擠壓或誤刪生產環境真實用戶日誌。

---

## [1.1.10] - 2026-08-19

### 🔍 活動日誌資料庫級別即時直查 (Database Search)
- **活動日誌升級為伺服器端資料庫直查 (Server-Side DB Search)**：
  - 後端 API 與前端搜尋升級為資料庫級別即時直查，打破原先僅限於前端 50 筆快取的過濾限制，可自全庫撈取數百筆相符歷史記錄。
  - 前端加入 350ms 防抖搜尋機制、Loading 轉圈動畫反饋與「🔍 找到 X 筆相符歷史紀錄」統計提示。

---

## [1.1.9] - 2026-08-19

### 🛠️ ORM 資料存取層模糊查詢正則轉換修復 (Prisma Layer Fix)
- **補齊 MongoDB 存取層運算子轉換**：
  - 在 `lib/prisma.ts` 的 `parseWhere` 補齊 `startsWith`、`endsWith`、`contains` 與 `mode: "insensitive"` 正則自動轉換與字元轉義，徹底杜絕 ReDoS 正則注入。

---

## [1.1.8] - 2026-08-19

### 🛡️ 活動日誌防清空與防篡改保護 (Anti-Wipe)
- **活動日誌不可清空機制**：
  - 停用後台手動清空日誌功能，後端 `DELETE /api/admin/logs` 回傳 403 禁用，移除前端清空按鈕，確保審計日誌不可被手動清空。

---

## [1.1.7] - 2026-08-19

### ⚙️ 全站 SessionProvider 包覆與 SSR 修復 (Architecture Fix)
- **全站 AuthProvider 包裝**：
  - 建立全站 `AuthProvider` 元件，修復 Next.js SSR 階段因 `useSession` 上下文未注入導致的 500 頁面載入失敗問題。

---

## [1.1.6] - 2026-08-19

### 🚀 註冊預填、動態牆跳轉修復與個人稱呼修改 (User Profile & Feed Flow)
- **修復已核准用戶進 /feed 誤跳轉問題**：Server 端直查 DB 狀態判定真實權威狀態，解決 JWT Cookie 緩存死鎖。
- **註冊頁自動預填**：自動帶入 Google 帳號名稱，並解析 URL `?code=` 自動預填邀請碼。
- **使用者個人稱呼修改**：新增 `PATCH /api/user/profile` 與導航列「✏️ 修改稱呼」彈窗。

---

## [1.1.5] - 2026-08-19

### 🎫 分組展示頁直通兌換與註冊升級 (Share Page Redeem)
- **分組展示頁支援新訪客直接兌換註冊**：訪客可直接在展示頁輸入邀請碼完成註冊開通或 pending 狀態升級。

---

## [1.1.4] - 2026-08-19

### 🔒 邀請碼安全刪除防護 (Safe Delete Rules)
- **邀請碼刪除安全防護**：邀請碼必須處於「已停用 (disabled: true)」狀態方可執行刪除，杜絕誤刪進行中的有效邀請碼。

---

## [1.1.3] - 2026-08-19

### 🎬 影片庫管理快速播放通道 (Admin UX)
- **影片管理列表快速播放**：後台影片管理清單中，編輯鉛筆旁新增「在外部新分頁播放」專屬按鈕。

---

## [1.1.2] - 2026-08-19

### 🎨 輸入邀請碼彈窗 100% 居中定位修復 (Modal Fix)
- **React Portal 視窗置中**：採用 React Portal 渲染輸入邀請碼彈窗，確保在任何頁面與滾動高度均精準垂直水平置中。

---

## [1.1.1] - 2026-08-19

### 📱 分組推廣與邀請碼分享彈窗尺寸放大 (UI Optimization)
- **推廣彈窗視覺優化**：放大推廣彈窗視窗尺寸、影片縮圖比例與文字排版，提升分享閱讀體驗。

---

## [1.1.0] - 2026-08-19

### 🚀 次版本重大功能發布 (Minor Release)
- **分組隨機 10 碼短分享 ID 系統 (Random Share ID)**：
  - 分組新增 10 碼隨機 URL-safe 識別碼（`shareId`），隱蔽 MongoDB 底層 ObjectId，支援公開展示頁（`/share/group/[shareId]`）與 Open Graph 社交動態預覽圖片生成（`/api/og/group/[shareId]`）。
- **免審核邀請碼自動解鎖與即時歡迎信 (Auto-Approve & Welcome Email)**：
  - 邀請碼支援 `autoApprove: true`，輸入即自動開通對應分組並即時寄發精美 HTML 歡迎信。
- **動態管理員名稱支援**：全站 Jacky 稱呼改為自資料庫動態讀取管理員名稱。

---

## [1.0.19] - 2026-08-18

### 🔒 安全性與權限隔離優化 (Security & Privacy)
- **Google OAuth 登入權限全面淨化與隔離**：
  - 將前台一般使用者登入之 OAuth 範圍精簡為純淨之 `openid email profile`，完全移除對一般訪客與會員的 `youtube.readonly` 敏感權限要求。
  - **徹底消除未經驗證警告**：一般使用者登入時僅提示授權基本姓名與 Email，不再跳出「這個應用程式未經 Google 驗證」警告畫面，大幅提升信任感與登入流暢度。
  - 頻道同步與影片抓取全面由後端伺服器級 `YOUTUBE_API_KEY` 獨立執行，達成最高標準的最小權限與職責分離原則。

---

## [1.0.18] - 2026-08-18

### 🚀 新增功能 (Features)
- **系統活動日誌管理中心 (System Activity Logs)**：
  - 新增專屬資料庫模型 `ActivityLog` 與後台管理 API（`/api/admin/logs`）。
  - 自動攔截並記錄使用者**登入（Login）、登出（Logout）、註冊審核申請（Register）**等完整軌跡。
  - **智慧客戶端作業系統與瀏覽器解析**：自動辨識並標註作業系統（Windows 10/11, macOS, iOS, Android, Linux）與瀏覽器（Chrome, Safari, Edge, Firefox, Opera），附帶獨立美觀之作業系統圖示徽章。
  - **即時強大搜尋與分類篩選**：支援即時搜尋 Email、姓名、IP 位址、作業系統 OS、瀏覽器與詳細說明，並支援按動作類型（全部／登入／登出／註冊）切換篩選。
  - **後台導航整合**：後台導航欄位新增 **📜「活動日誌」** 按鈕，提供重新整理、清空日誌與詳細時間軸展示。

---

## [1.0.17] - 2026-08-18

### 🎨 介面與視覺體驗優化 (Style & UX)
- **全新高階暗黑玻璃質感預設影片縮圖 (Default Fallback Thumbnail)**：
  - 生成並導入專屬高階暗黑玻璃擬態、琥珀金光暈與幾何光影之 16:9 預設縮圖（`/images/default-thumbnail.jpg`）。
  - 全面套用於後台管理影片列表、會員影片動態牆、公開首頁卡片與自訂影片卡片組件。
  - 導入全域 `onError` 圖片自動偵錯降級機制，當 YouTube 私人影片縮圖無法取得或載入失敗（404）時，自動平滑切換至專屬預設縮圖，徹底告別破圖或空白問題。

### 🚀 影片播放頁體驗升級 (Features)
- **影片播放頁強化 YouTube 直達通道與私人影片指引**：
  - 播放器上方新增「在 YouTube 開啟觀看」直達按鈕。
  - 提供私人影片跨域安全性政策與改設「不公開 (Unlisted)」之最佳實踐提示。

---

## [1.0.16] - 2026-08-18

### 🐛 問題修復與優化 (Fixes & UX)
- **編輯影片設定分組點選與即時新增機制強化**：
  - 修復編輯影片設定彈窗內分類點選與儲存邏輯，確保選取狀態精準同步至影片模型。
  - 支援「＋ 新增分類」即時智慧判定：若輸入既有分類名稱，自動完成勾選與指派；若為新分類則透過 API 建立並自動加入授權分組。
  - 彈窗內新增專屬即時狀態提示橫幅（成功/錯誤），操作回饋更直覺。
  - 優化 `SmartDateInput` 智慧溢位重置與點擊全選機制，支援直接鍵入 `10`、`11`、`12` 月並自動跳格。

---

## [1.0.15] - 2026-08-18

### 🚀 新增功能 (Features)
- **SmartDateInput 智慧日期輸入組件**：
  - 西元年份限制 4 位數，輸滿自動將焦點跳轉至月份。
  - 支援自然雙位數輸入（01-12 月、01-31 日），輸入滿 2 位數或按下 `/`、`-`、`Enter` 分隔鍵即自動跳下一格。
  - 支援失焦（Blur）自動格式化補零、退格鍵（Backspace）跨欄位導覽與剪貼簿直接貼上多種日期格式。
  - 整合原生日曆選擇器（Calendar Picker）與一鍵清除按鈕。
  - 全面套用於後台「新增影片」與「編輯影片」之拍攝日期輸入欄位。

### 🎨 介面優化 (Style & UX)
- **全新醒目頁尾 (Enhanced Premium Footer)**：
  - 移除首頁與頁尾重疊之隱私宣告欄位。
  - 升級為大氣玻璃擬態設計，醒目呈現「📘 應用程式與 API 政策」、「🔒 隱私權政策」與「📜 服務條款」三大核心按鈕。
  - 清楚標註 Google 帳戶安全性撤銷授權說明與版權宣告。

---

## [1.0.14] - 2026-08-18

### 🚀 新增功能 (Features)
- 初步引入 `SmartDateInput` 組件與基礎跳格結構。

---

## [1.0.13] - 2026-08-18

### 🎨 介面優化 (Style & UX)
- 調整首頁佈局，清理多餘橫幅宣告，統一集中至全站頁尾呈現。

---

## [1.0.12] - 2026-08-17

### 🐛 問題修復 (Bug Fixes)
- **統一全站影片排序規則 (`getEffectiveTimestamp`)**：
  - 優先取得拍攝時間（`shootingDate`），若無拍攝時間則自動回退取出發布/上傳時間（`publishedAt`）。
  - 徹底解決混合日期狀態下解析出 `NaN` 導致排序異常的問題。
  - 同步套用於後台影片管理清單、前台影片動態牆與首頁精選公開專區之新舊排序與年份篩選。

---

## [1.0.11] - 2026-08-17

### 🚀 新增功能 (Features)
- **首頁精選公開影音專區時間軸升級**：
  - 將首頁「精選公開影音專區」升級為中央發光時間軸與雙欄交錯緊湊排列。
  - 支援「由新到舊」與「由舊到新」即時排序切換。
  - 提供「時間軸」與「網格圖卡」一鍵切換模式。

---

## [1.0.10] - 2026-08-17

### 🎨 介面優化 (Style & UX)
- **雙欄交錯緊湊時間軸**：
  - 移除對側空置佔位區塊，採用左右交錯緊湊佈局。
  - 拍攝/發布時間整合標註於影片卡片頂部 Header，保持畫面飽滿且間距舒適。

---

## [1.0.9] - 2026-08-17

### 🚀 新增功能 (Features)
- **影片動態牆時間軸架構 (`/feed`)**：
  - 全新設計中央時間軸線、發光節點、左右延伸連接橫線。
  - 預設由新到舊排列，支援切換由舊到新。

---

## [1.0.8] - 2026-08-17

### 📚 文件與政策 (Docs & Compliance)
- 於 `/about` 與 `/privacy` 頁面完整補充中英雙語之「角色權限分級宣告（Role-Based Access Disclosure）」：
  - 明確宣告一般訪客與會員僅需基本登入資訊（`openid, email, profile`），絕不存取任何 YouTube 敏感權限。
  - `youtube.readonly` 權限嚴格僅供系統管理員同步創作者本人頻道影音。

---

## [1.0.7] - 2026-08-17

### 🛡️ 資料保護 (Security & Data Integrity)
- YouTube 頻道影音同步時，完整保護站內已由管理員自訂修改之影片標題，避免二次同步時遭 YouTube 原始標題覆蓋。

---

## [1.0.6] - 2026-08-17

### 🐛 問題修復 (Bug Fixes)
- 修復編輯影片與新增影片彈窗中分組標籤點選與儲存邏輯，改用標準按鈕事件切換提升操作穩定度。

---

## [1.0.5] - 2026-08-17

### 🚀 新增功能 (Features)
- 編輯影片設定彈窗與新增單部影片彈窗內支援即時新增自訂分類分組。

---

## [1.0.4] - 2026-08-17

### 🚀 新增功能 (Features)
- 影片庫管理清單預設為網格圖卡呈現，預設由新到舊排序，並支援由舊到新切換。
- 後台分組管理支援直接修改分類名稱。
- 批次指派分組彈窗內支援即時新增分類。

---

## [1.0.3] - 2026-08-17

### ⚡ 流程簡化 (Refactor)
- 移除手動輸入頻道 ID 的齒輪設定彈窗，簡化為自動呼叫 API 進行頻道影音同步。

---

## [1.0.2] - 2026-08-17

### 🚀 新增功能 (Features)
- 批次管理時，點擊影片縮圖或卡片任一處即可直接切換打勾選取狀態，大幅提升批次分類操作流暢度。

---

## [1.0.1] - 2026-08-17

### 🚀 新增功能 (Features)
- 影片庫管理新增「尚未分類」快捷篩選條件。
- 支援批次打勾指派分組功能。
- 修復用戶審核名冊邏輯，確保管理員永遠名列其中並具備安全防護。

---

## [1.0.0] - 2026-08-17

### 🎉 初始正式發布 (Initial Release)
- 純淨開源架構（Next.js 16 + React 19 + Prisma + MongoDB）。
- Google OAuth 2.0 身分驗證與角色權限管控（訪客、待審核會員、正式會員、管理員）。
- 創作者 YouTube 頻道同步與 YouTube Embedded Player 安全播放整合。
- 邀請碼機制、分組權限隔離與全自動 CI/CD / Docker 容器化部署環境。
